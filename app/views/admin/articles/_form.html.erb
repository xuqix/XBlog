<%= form_for([:admin, @article]) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<p>
  <%= f.label :title %><br>
  <%= f.text_field :title %>
</p>
<p>
  <%= f.label :text %><br>
  <%= f.text_area :content %>
</p>
<p>
  <%= f.submit (defined?(text) ? text : nil) %>
</p>
<% end %>
